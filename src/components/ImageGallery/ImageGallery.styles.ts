import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  image: {
    width: 335,
    height: 400,
    borderRadius: 10,
  },
  pagination: {
    bottom: -10,
    left: 0,
    right: 0,
    position: 'absolute',
  },
});
